{
  "name": "@dolphin/monorepo",
  "version": "1.0.3",
  "description": "Convert lark cloud documents to markdown",
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "clean": "turbo run clean",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "format-check": "turbo run format-check",
    "release": "turbo run format-check lint check test build && changeset version"
  },
  "keywords": [
    "lark",
    "markdown"
  ],
  "author": "Junji Lu",
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "eslint": "^8.57.0",
    "happy-dom": "^13.7.8",
    "prettier": "^3.2.5",
    "turbo": "^1.12.5",
    "typescript": "^5.4.2",
    "vite": "^5.1.6",
    "vitest": "^1.3.1"
  },
  "dependencies": {
    "core-js": "3.36.0"
  }
}
