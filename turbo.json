{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**", "bundles/**"],
      "dependsOn": ["clean", "^build"]
    },
    "clean": {
      "cache": false
    },
    "check": {
      "dependsOn": ["^build", "^check"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "test": {
      "dependsOn": ["^build", "^test"]
    },
    "format": {},
    "format-check": {}
  }
}
